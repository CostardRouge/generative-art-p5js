{
    "name": "generative-art-p5js",
    "scripts": {
        "deploy": "gh-pages -d .",
        "predeploy": "npm --prefix ./sketches-explorer run deploy"
    },
    "devDependencies": {
        "gh-pages": "^3.2.3"
    }
}