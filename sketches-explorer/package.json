{
  "name": "static-explorer",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:tree": "node ./scripts/build-tree.js ../sketches src/tree.json",
    "deploy": "npm run build:tree && npm run build -- --base=/generative-art-p5js/"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^1.0.7",
    "vite": "^2.7.2"
  }
}
